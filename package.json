{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "solve-screenshot",
  "title": "Solve Screenshot with AI",
  "description": "Capture a screenshot, ask Claude or Codex to solve the question in it, and copy the answer to the clipboard.",
  "icon": "extension-icon.png",
  "author": "aksay_kanthan",
  "license": "MIT",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Productivity",
    "Developer Tools"
  ],
  "commands": [
    {
      "name": "solve-screenshot",
      "title": "Solve Screenshot with AI",
      "subtitle": "Solve screenshot and copy to clipboard",
      "description": "Capture a screenshot, ask Claude or Codex to solve the question in it, and copy the answer to the clipboard.",
      "mode": "no-view"
    }
  ],
  "preferences": [
    {
      "name": "solver",
      "type": "dropdown",
      "required": false,
      "title": "Screenshot Solver",
      "description": "Choose the AI CLI to solve screenshots.",
      "default": "codex",
      "data": [
        {
          "title": "Codex",
          "value": "codex"
        },
        {
          "title": "Claude Code",
          "value": "claude"
        }
      ]
    }
  ],
  "scripts": {
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "build": "ray build",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  },
  "dependencies": {
    "@raycast/api": "^1.103.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
